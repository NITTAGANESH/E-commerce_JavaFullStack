import { Easy2ShopValidators } from './easy2-shop-validators';

describe('Easy2ShopValidators', () => {
  it('should create an instance', () => {
    expect(new Easy2ShopValidators()).toBeTruthy();
  });
});
